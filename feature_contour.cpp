#include "feature_contour.h"

void score_by_contour(cv::Mat& visited, cv::Mat& img, int  direction[], cv::Point basePoint, vector<int> params, float* score)
{
}
void score_by_texture(cv::Mat& visited, cv::Mat& img, int direction[], cv::Point basePoint, vector<int> params, float* score)
{
}
